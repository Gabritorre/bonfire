services:
  db:
    image: postgres:15
    environment:
     POSTGRES_PASSWORD: 1234

  flask:
    build:
      context: .
    restart: on-failure
    ports:
      - "5000:5000"
    depends_on:
      - db

